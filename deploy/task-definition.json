{
  "family": "pay2play",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "pidMode": "task",
  "cpu": "1024",
  "memory": "2048",
  "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${EXECUTION_ROLE_NAME}",
  "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${TASK_ROLE_NAME}",
  "containerDefinitions": [
    {
      "name": "frontend",
      "image": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/pay2play-frontend:${IMAGE_TAG}",
      "essential": true,
      "portMappings": [{ "containerPort": 80, "protocol": "tcp" }],
      "environment": [
        { "name": "BACKEND_HOST", "value": "127.0.0.1" },
        { "name": "BACKEND_PORT", "value": "8000" }
      ],
      "dependsOn": [{ "containerName": "backend", "condition": "START" }],
      "logConfiguration": {
        "logDriver": "awsfirelens",
        "options": {
          "Name": "datadog",
          "Host": "http-intake.logs.${DD_SITE}",
          "TLS": "on",
          "provider": "ecs",
          "dd_service": "pay2play-frontend",
          "dd_source": "nginx",
          "dd_message_key": "log",
          "dd_tags": "env:${DD_ENV}"
        },
        "secretOptions": [
          { "name": "apikey", "valueFrom": "${DD_API_KEY_SECRET_ARN}" }
        ]
      }
    },
    {
      "name": "backend",
      "image": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/pay2play-backend:${IMAGE_TAG}",
      "essential": true,
      "portMappings": [{ "containerPort": 8000, "protocol": "tcp" }],
      "environment": [
        { "name": "SEED_ON_START", "value": "true" },
        { "name": "DD_ENV", "value": "${DD_ENV}" },
        { "name": "DD_SERVICE", "value": "pay2play-backend" },
        { "name": "DD_VERSION", "value": "0.1.0" },
        { "name": "DD_LOGS_INJECTION", "value": "true" },
        { "name": "DD_RUNTIME_METRICS_ENABLED", "value": "true" },
        { "name": "DD_DBM_PROPAGATION_MODE", "value": "full" },
        { "name": "DD_TRACE_AGENT_URL", "value": "unix:///var/run/datadog/apm.socket" },
        { "name": "DD_DOGSTATSD_URL", "value": "unix:///var/run/datadog/dsd.socket" },
        { "name": "DD_LLMOBS_ML_APP", "value": "pay2play-chatbot" },
        { "name": "OPENAI_MODEL", "value": "gpt-5" }
      ],
      "secrets": [
        { "name": "DATABASE_URL", "valueFrom": "${DATABASE_URL_SECRET_ARN}" },
        { "name": "JWT_SECRET", "valueFrom": "${JWT_SECRET_ARN}" },
        { "name": "OPENAI_API_KEY", "valueFrom": "${OPENAI_API_KEY_SECRET_ARN}" }
      ],
      "mountPoints": [
        { "containerPath": "/var/run/datadog", "sourceVolume": "dd-sockets" }
      ],
      "logConfiguration": {
        "logDriver": "awsfirelens",
        "options": {
          "Name": "datadog",
          "Host": "http-intake.logs.${DD_SITE}",
          "TLS": "on",
          "provider": "ecs",
          "dd_service": "pay2play-backend",
          "dd_source": "python",
          "dd_message_key": "log",
          "dd_tags": "env:${DD_ENV}"
        },
        "secretOptions": [
          { "name": "apikey", "valueFrom": "${DD_API_KEY_SECRET_ARN}" }
        ]
      }
    },
    {
      "name": "datadog-agent",
      "image": "public.ecr.aws/datadog/agent:latest",
      "essential": true,
      "environment": [
        { "name": "DD_SITE", "value": "${DD_SITE}" },
        { "name": "ECS_FARGATE", "value": "true" },
        { "name": "DD_APM_ENABLED", "value": "true" },
        { "name": "DD_APM_RECEIVER_SOCKET", "value": "/var/run/datadog/apm.socket" },
        { "name": "DD_DOGSTATSD_SOCKET", "value": "/var/run/datadog/dsd.socket" },
        { "name": "DD_PROCESS_AGENT_ENABLED", "value": "true" },
        { "name": "DD_ENV", "value": "${DD_ENV}" },
        { "name": "DD_SYSTEM_PROBE_NETWORK_ENABLED", "value": "true" },
        { "name": "DD_NETWORK_CONFIG_ENABLE_EBPFLESS", "value": "true" }
      ],
      "secrets": [
        { "name": "DD_API_KEY", "valueFrom": "${DD_API_KEY_SECRET_ARN}" }
      ],
      "mountPoints": [
        { "containerPath": "/var/run/datadog", "sourceVolume": "dd-sockets" }
      ],
      "linuxParameters": {
        "capabilities": {
          "add": ["SYS_PTRACE"]
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "agent health"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 15
      },
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/pay2play",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "datadog-agent",
          "awslogs-create-group": "true"
        }
      }
    },
    {
      "name": "combined-fe",
      "image": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/pay2play-combined-fe:${IMAGE_TAG}",
      "essential": false,
      "environment": [
        { "name": "DD_ENV", "value": "${DD_ENV}" },
        { "name": "DD_SERVICE", "value": "pay2play-combined-fe" },
        { "name": "DD_VERSION", "value": "0.1.0" },
        { "name": "DD_LOGS_INJECTION", "value": "true" },
        { "name": "DD_TRACE_AGENT_URL", "value": "unix:///var/run/datadog/apm.socket" },
        { "name": "DD_DOGSTATSD_URL", "value": "unix:///var/run/datadog/dsd.socket" }
      ],
      "mountPoints": [
        { "containerPath": "/var/run/datadog", "sourceVolume": "dd-sockets" }
      ],
      "dependsOn": [{ "containerName": "datadog-agent", "condition": "START" }],
      "logConfiguration": {
        "logDriver": "awsfirelens",
        "options": {
          "Name": "datadog",
          "Host": "http-intake.logs.${DD_SITE}",
          "TLS": "on",
          "provider": "ecs",
          "dd_service": "pay2play-combined-fe",
          "dd_source": "python",
          "dd_message_key": "log",
          "dd_tags": "env:${DD_ENV}"
        },
        "secretOptions": [
          { "name": "apikey", "valueFrom": "${DD_API_KEY_SECRET_ARN}" }
        ]
      }
    },
    {
      "name": "log_router",
      "image": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/pay2play-logrouter:${IMAGE_TAG}",
      "essential": true,
      "firelensConfiguration": {
        "type": "fluentbit",
        "options": {
          "enable-ecs-log-metadata": "true",
          "config-file-type": "file",
          "config-file-value": "/extra.conf"
        }
      },
      "environment": [
        { "name": "DD_SITE", "value": "${DD_SITE}" },
        { "name": "DD_ENV", "value": "${DD_ENV}" }
      ],
      "secrets": [
        { "name": "DD_API_KEY", "valueFrom": "${DD_API_KEY_SECRET_ARN}" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/pay2play",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "log-router",
          "awslogs-create-group": "true"
        }
      }
    }
  ],
  "volumes": [{ "name": "dd-sockets" }]
}